<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rumofuture.nemo.repository.mapper.PageMapper">

    <resultMap id="pageMap" type="com.rumofuture.nemo.model.domain.Page">
        <id column="id" property="id"/>
        <result property="objectId" column="object_id"/>
        <result property="status" column="status"/>
        <result property="createAt" column="create_at"/>
        <result property="updateAt" column="update_at"/>
        <result property="imageUrl" column="image_url"/>
    </resultMap>

    <!-- 保存 -->
    <insert id="insert" parameterType="page" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO page(object_id, status, create_at, update_at, image_url, book_id)
        VALUES(#{objectId}, #{status}, #{createAt}, #{updateAt}, #{book.id}, #{imageUrl})
    </insert>

    <!-- 更新 -->
    <update id="update" parameterType="page">
        UPDATE user
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="password != null">password = #{password},</if>
            <if test="orgId != null">org_id = #{orgId},</if>
            <if test="orgCode != null">org_code = #{orgCode},</if>
            <if test="orgName != null">org_name = #{orgName},</if>
            <if test="updateTime != null">update_time = #{updateTime}</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 查找 -->
    <select id="selectOne" parameterType="int" resultMap="pageMap">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <!--查找-->
    <select id="selectByMobilePhoneNumber" parameterType="string" resultMap="pageMap">
        SELECT * FROM user WHERE mobile_phone_number = #{mobilePhoneNumber}
    </select>
</mapper>